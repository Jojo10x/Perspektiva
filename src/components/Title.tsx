import styles from "../../styles/Title.module.scss";
const Title = () => {
  return (
    <div className={styles.bg_move}>
      <i className={`$styles.fas fa-palette`}></i>
      <h1>1% Better</h1>
    </div>
  );
};

export default Title;
